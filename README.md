# Bakery

A logger for bakes

## Getting started

1. Create and active python virtual env

```bash
python3 -m venv __venv__
source __venv__/bin/activate
```

2. Install dependences
```bash
pip3 install Phidget22 influxdb-client pyyaml python-daemon
```

systemctl enable pasqwatch

